/**
 * @fileoverview Audio processing pipeline with lunar influences
 * @author Hexbloop Audio Labs
 * 
 * Pipeline: Input → Sox (mystical effects) → FFmpeg (mastering) → MP3 with embedded artwork
 */

const ffmpeg = require('fluent-ffmpeg');
const { spawn } = require('child_process');
const path = require('path');
const fs = require('fs');
const os = require('os');
const LunarProcessor = require('./lunar-processor');
const VibrantRefinedArtworkGenerator = require('./artwork-generator-vibrant-refined');
// Legacy generators moved to old-generators/
const MetadataEmbedder = require('./metadata-embedder');
const AudioAnalyzer = require('./audio-analyzer');
const { getPreferencesManager } = require('./menu/preferences');
const binaries = require('./binary-resolver');

// Configure fluent-ffmpeg with resolved binary paths (bundled or system)
if (binaries.ffmpeg.path) {
    ffmpeg.setFfmpegPath(binaries.ffmpeg.path);
    console.log(`🎬 FFmpeg: ${binaries.ffmpeg.bundled ? '(bundled)' : '(system)'} ${binaries.ffmpeg.path}`);
}
if (binaries.ffprobe.path) {
    ffmpeg.setFfprobePath(binaries.ffprobe.path);
}

class AudioProcessor {
    // Formats the whole pipeline (mastering, conversion, metadata, artwork)
    // actually supports end-to-end. The preferences UI offers exactly these;
    // anything else in a settings file falls back to mp3.
    static SUPPORTED_OUTPUT_FORMATS = ['mp3', 'wav', 'flac'];

    static resolveOutputFormat(settings) {
        const format = settings?.output?.format;
        return this.SUPPORTED_OUTPUT_FORMATS.includes(format) ? format : 'mp3';
    }

    static async processFile(inputPath, outputPath) {
        console.log(`🎵 Processing: ${path.basename(inputPath)} -> ${path.basename(outputPath)}`);
        
        // Get user preferences for processing configuration
        const preferencesManager = getPreferencesManager();
        const processingConfig = preferencesManager.getProcessingConfig();
        const settings = preferencesManager.getSettings();
        const artworkSettings = settings.artwork || {};
        
        // Extract the name from outputPath (already generated by main.js)
        // Remove .mp3 extension to get the mystical name
        let finalName = path.parse(path.basename(outputPath)).name;
        let customMetadata = null;
        
        // Handle custom metadata if provided
        if (processingConfig.stages.naming === 'custom' && processingConfig.metadata) {
            customMetadata = processingConfig.metadata;
            console.log(`📝 Using custom metadata`);
        }
        
        const lunarInfluence = processingConfig.stages.compressing ? LunarProcessor.getInfluencedParameters() : null;
        const compressionProfile = lunarInfluence ? {
            overdrive: lunarInfluence.overdrive,
            bass: lunarInfluence.bass,
            treble: lunarInfluence.treble,
            echo: lunarInfluence.echo,
            compand: lunarInfluence.compand
        } : null;
        
        // Create unique temp directory for this processing job
        const uniqueId = `${Date.now()}_${Math.random().toString(36).slice(2, 9)}`;
        const tempDir = await fs.promises.mkdtemp(path.join(os.tmpdir(), `hexbloop-${uniqueId}-`));

        // Temp files for processing stages (scoped per run)
        const tempFile = path.join(tempDir, 'temp_audio.aif');
        const processedFile = path.join(tempDir, 'temp_processed.mp3');

        // Use Vibrant Refined Artwork Generator for maximum variety
        const artworkResolution = this.clampNumber(artworkSettings.resolution, 256, 4096, 1000);
        const artworkGenerator = new VibrantRefinedArtworkGenerator({
            width: artworkResolution,
            height: artworkResolution
        });
        const metadataEmbedder = new MetadataEmbedder();
        console.log('🎨 Using Vibrant Refined Generator - Full audio responsiveness with proper color theory!');
        try {
            let currentFile = inputPath;
            let artworkResult = null;
            
            // Step 1: Sox processing (conditional)
            if (compressionProfile) {
                console.log(`🎛️ Applying mystical compression: ${lunarInfluence.description}`);
                await this.processSox(currentFile, tempFile, compressionProfile);
                currentFile = tempFile;
            } else {
                console.log('⏭️ Skipping compression stage');
            }
            
            // Step 2: FFmpeg mastering (conditional)
            if (processingConfig.stages.mastering) {
                console.log('🎚️ Applying mystical mastering...');
                const masteringInput = currentFile;
                const masteringOutput = processingConfig.stages.compressing ? processedFile : tempFile;
                await this.processFFmpeg(masteringInput, masteringOutput, settings);
                currentFile = masteringOutput;
            } else {
                console.log('⏭️ Skipping mastering stage');
                // If no mastering but we have a sox-processed file, convert it
                // to the user's chosen output format (not hardcoded mp3)
                if (currentFile !== inputPath) {
                    await this.convertToFormat(currentFile, processedFile, settings);
                    currentFile = processedFile;
                }
            }
            
            // Step 3: Generate artwork (conditional)
            if (processingConfig.stages.coverArt) {
                console.log('🎨 Generating mystical artwork...');
                const useAudioFeatures = artworkSettings.useAudioFeatures !== false;
                const useMoonPhase = artworkSettings.useMoonPhase !== false;
                const useFileNameHints = artworkSettings.useFileNames !== false;
                const defaultStyle = artworkSettings.defaultStyle && artworkSettings.defaultStyle !== 'auto'
                    ? artworkSettings.defaultStyle
                    : null;
                const energySensitivity = this.clampNumber(artworkSettings.energySensitivity, 0, 100, 50) / 100;
                const tempoInfluence = this.clampNumber(artworkSettings.tempoInfluence, 0, 100, 50) / 100;
                const colorVariation = this.clampNumber(artworkSettings.colorVariation, 0, 100, 50) / 100;

                // Respect artwork format setting (png or jpg)
                const artworkFormat = artworkSettings.imageFormat || 'png';
                const artworkExt = artworkFormat === 'jpg' ? 'jpg' : 'png';
                const artworkPath = path.join(tempDir, `${finalName.replace(/[^a-zA-Z0-9]/g, '_')}_artwork.${artworkExt}`);

                // Get moon phase for artwork generation
                const moonData = LunarProcessor.getMoonPhase();
                const moonPhase = useMoonPhase && typeof moonData === 'object'
                    ? moonData.phase
                    : useMoonPhase
                        ? moonData
                        : 0.5;
                
                // Analyze audio for visual features
                let audioFeatures = null;
                if (useAudioFeatures) {
                    try {
                    console.log('🎵 Analyzing audio for visual features...');
                    audioFeatures = await AudioAnalyzer.analyzeAudio(inputPath);
                    console.log(`   Energy: ${(audioFeatures.energy * 100).toFixed(0)}% | Tempo: ${audioFeatures.tempo} BPM`);
                    } catch (analysisError) {
                        console.log('⚠️ Audio analysis skipped:', analysisError.message);
                    }
                } else {
                    console.log('⏭️ Skipping audio-driven artwork analysis');
                }

                const effectiveAudioEnergy = audioFeatures
                    ? this.interpolate(0.5, audioFeatures.energy, energySensitivity)
                    : 0.5;
                const effectiveTempo = audioFeatures
                    ? Math.round(this.interpolate(120, audioFeatures.tempo, tempoInfluence))
                    : 120;
                
                // Auto-select style based on processing characteristics
                // Enhanced generator has 8 styles: neon-plasma, cosmic-flow, vapor-dream, cyber-matrix, sunset-liquid, electric-storm, crystal-prism, ocean-aurora
                let artStyle = defaultStyle;
                if (!artStyle && useFileNameHints && (finalName.toLowerCase().includes('cosmic') || finalName.toLowerCase().includes('astral') || 
                    finalName.toLowerCase().includes('star') || finalName.toLowerCase().includes('space'))) {
                    artStyle = 'cosmic-flow';
                } else if (!artStyle && useFileNameHints && (finalName.toLowerCase().includes('electric') || finalName.toLowerCase().includes('power') ||
                           finalName.toLowerCase().includes('volt') || finalName.toLowerCase().includes('surge'))) {
                    artStyle = 'electric-storm';
                } else if (!artStyle && useFileNameHints && (finalName.toLowerCase().includes('neon') || finalName.toLowerCase().includes('plasma') ||
                           finalName.toLowerCase().includes('glow') || finalName.toLowerCase().includes('light'))) {
                    artStyle = 'neon-plasma';
                } else if (!artStyle && useFileNameHints && (finalName.toLowerCase().includes('wave') || finalName.toLowerCase().includes('flow') ||
                           finalName.toLowerCase().includes('liquid') || finalName.toLowerCase().includes('dream'))) {
                    artStyle = 'sunset-liquid';
                } else if (!artStyle && useFileNameHints && (finalName.toLowerCase().includes('crystal') || finalName.toLowerCase().includes('gem') ||
                           finalName.toLowerCase().includes('diamond') || finalName.toLowerCase().includes('hex'))) {
                    artStyle = 'crystal-prism';
                } else if (!artStyle && useFileNameHints && (finalName.toLowerCase().includes('vapor') || finalName.toLowerCase().includes('retro') ||
                           finalName.toLowerCase().includes('synth') || finalName.toLowerCase().includes('80s'))) {
                    artStyle = 'vapor-dream';
                } else if (!artStyle && useFileNameHints && (finalName.toLowerCase().includes('ocean') || finalName.toLowerCase().includes('water') ||
                           finalName.toLowerCase().includes('sea') || finalName.toLowerCase().includes('aurora'))) {
                    artStyle = 'ocean-aurora';
                } else if (!artStyle && useFileNameHints && (finalName.toLowerCase().includes('cyber') || finalName.toLowerCase().includes('matrix') ||
                           finalName.toLowerCase().includes('digital') || finalName.toLowerCase().includes('tech'))) {
                    artStyle = 'cyber-matrix';
                } else if (!artStyle && audioFeatures && effectiveAudioEnergy > 0.7) {
                    artStyle = 'neon-plasma'; // High energy audio
                } else if (!artStyle && audioFeatures && effectiveAudioEnergy < 0.3) {
                    artStyle = 'ocean-aurora'; // Low energy = calm auroras
                } else if (!artStyle) {
                    // Default to style based on moon phase for variety
                    const styles = ['neon-plasma', 'cosmic-flow', 'vapor-dream', 'cyber-matrix', 'sunset-liquid', 'electric-storm', 'crystal-prism', 'ocean-aurora'];
                    const moonIndex = Math.floor((moonPhase || Math.random()) * styles.length) % styles.length;
                    artStyle = styles[moonIndex];
                }
                
                // Generate canvas-based artwork with moon phase, style, and audio features
                const canvas = await artworkGenerator.generate({
                    style: artStyle,
                    title: finalName,
                    moonPhase: moonPhase,
                    seed: Date.now(),
                    audioEnergy: effectiveAudioEnergy,
                    tempo: effectiveTempo,
                    colorVariation
                });
                
                // Save the artwork in the user's chosen format
                await artworkGenerator.saveToFile(artworkPath, artworkExt, {
                    compressionLevel: this.clampNumber(artworkSettings.compressionLevel, 0, 9, 6),
                    resolution: artworkResolution
                });
                
                artworkResult = {
                    pngPath: artworkPath,
                    style: artStyle || 'cosmic-flow'
                };
                
                console.log(`🎨 Generated ${artworkResult.style} style artwork (moon phase: ${(moonPhase * 100).toFixed(0)}%)`);
            } else {
                console.log('⏭️ Skipping artwork generation');
            }
            
            // Step 4: Embed metadata (always happens, but content varies)
            console.log('📋 Embedding metadata...');
            const metadataForEmbedding = customMetadata ? {
                title: customMetadata.title || path.parse(path.basename(inputPath)).name,
                artist: customMetadata.artist,
                album: customMetadata.album,
                year: customMetadata.year,
                genre: customMetadata.genre,
                comment: customMetadata.comment || '',
            } : (() => {
                const now = new Date();
                const moonData = LunarProcessor.getMoonPhase();
                const timeWord = now.getHours() < 6 ? 'Nocturnal'
                    : now.getHours() < 12 ? 'Morning'
                    : now.getHours() < 18 ? 'Afternoon'
                    : 'Evening';

                // Dynamic album from a pool — rotates by moon phase
                const albums = [
                    'Lunar Transmutations', 'Tape Degradation Studies',
                    'Cassette Grimoire', 'Magnetic Rituals',
                    'Spectral Dubs & Glitches', 'Crushed Frequencies',
                    'Hexbloop Sessions', 'Chaos Magic Dubs',
                ];
                const albumIdx = Math.floor(moonData.phase * albums.length) % albums.length;

                // Genre from processing characteristics
                const genres = [
                    'Electronic', 'Experimental', 'Ambient',
                    'Lo-Fi', 'Noise', 'Drone',
                    'Industrial', 'Dark Ambient',
                ];
                const genreIdx = Math.floor((moonData.illumination || 0.5) * genres.length) % genres.length;

                return {
                    title: finalName,
                    artist: 'Hexbloop',
                    album: albums[albumIdx],
                    year: now.getFullYear(),
                    date: `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`,
                    genre: genres[genreIdx],
                    comment: `${moonData.name} | ${timeWord} session | tape-cassette processed`,
                };
            })();
            
            // If no processing was done, convert the original file
            if (currentFile === inputPath) {
                await this.convertToFormat(inputPath, processedFile, settings);
                currentFile = processedFile;
            }
            
            await metadataEmbedder.embedMetadata(
                currentFile,
                outputPath,
                metadataForEmbedding,
                artworkResult?.pngPath
            );

            // Artwork files are now in tempDir and cleaned up automatically by the finally block

            console.log(`✅ Successfully processed: ${path.basename(outputPath)}`);
            return {
                success: true,
                finalName,
                artwork: artworkResult,
                processingConfig,
                metadata: metadataForEmbedding
            };

        } finally {
            // Clean up temp directory and all its contents
            try {
                if (fs.existsSync(tempDir)) {
                    await fs.promises.rm(tempDir, { recursive: true, force: true });
                    console.log('🧹 Cleaned up temp directory');
                }
            } catch (cleanupError) {
                console.log('⚠️  Could not clean up temp directory:', cleanupError.message);
            }
        }
    }
    
    static buildCompandCurve(ratio) {
        // Parametrized tape-glue transfer: unity gain below the knee, 1/ratio above.
        // Transfer points sit 18dB apart so the 6dB soft knee can't make them
        // collide — sox rejects transfers whose points overlap after knee
        // smoothing ("input values must be strictly increasing").
        // ratio 2 → light glue, ratio 4 → heavy tape squash
        const clamped = Math.max(2, Math.min(4, ratio));
        const knee = Math.round(-6 * clamped);            // -12 … -24 dB knee point
        const floor = knee - 18;                          // unity anchor below the knee
        const ceil = Math.round(knee - knee / clamped);   // 0dB in → compressed out
        return `6:${floor},${floor},${knee},${knee},0,${ceil}`;
    }

    /**
     * Tape-cassette style sox chain:
     * fixed headroom → saturation → EQ → space → tape glue → normalize → dither
     * (gain -h/-r headroom *tracking* breaks across overdrive on real sox
     *  builds, so headroom is a fixed -4dB and reclaim is a -1dBFS normalize)
     */
    static buildSoxArgs(inputPath, outputPath, influences) {
        const ratio = influences.compand.ratio || 3;
        const compandCurve = this.buildCompandCurve(ratio);
        return [
            inputPath,
            outputPath,
            'gain', '-4',                             // Headroom before saturation
            'overdrive', influences.overdrive.toString(), '2.0',  // Tape saturation (lower gain=warmer)
            'bass', `+${influences.bass}`,            // Low warmth
            'treble', influences.treble >= 0 ? `+${influences.treble}` : influences.treble.toString(),
            'echo', influences.echo.delay.toString(), influences.echo.decay.toString(), '6', '0.03',  // Subtle space
            'compand', `${influences.compand.attack},0.2`, compandCurve, '-3', '-40', '0.05',  // Tape glue
            'gain', '-n', '-1',                       // Normalize to -1dBFS (deterministic reclaim)
            'rate', '44100',
            'dither'
        ];
    }

    static async processSox(inputPath, outputPath, influences) {
        return new Promise((resolve, reject) => {
            // Ensure influences object exists with defaults
            if (!influences) {
                influences = {
                    overdrive: 2.0,
                    bass: 1.5,
                    treble: 0.5,
                    echo: { delay: 0.3, decay: 0.05 },
                    compand: { attack: 0.06, ratio: 3 }
                };
            }

            const soxBin = binaries.sox.path || 'sox';
            const soxProcess = spawn(soxBin, this.buildSoxArgs(inputPath, outputPath, influences));
            
            let stderr = '';
            // 'error' (spawn failure) and 'close' can BOTH fire — guard so the
            // FFmpeg fallback doesn't run twice against the same output file
            let settled = false;

            soxProcess.stderr.on('data', (data) => {
                stderr += data.toString();
            });

            const fallbackToFFmpeg = (reason) => {
                if (settled) return;
                settled = true;
                console.log(`⚠️ ${reason}, using FFmpeg for all processing...`);
                this.processWithFFmpegOnly(inputPath, outputPath, influences).then(resolve).catch(reject);
            };

            soxProcess.on('close', (code) => {
                if (settled) return;
                if (code === 0) {
                    settled = true;
                    console.log('✨ Sox processing complete');
                    resolve();
                } else {
                    if (stderr) console.log('   sox stderr:', stderr.split('\n')[0]);
                    fallbackToFFmpeg(`Sox exited with code ${code}`);
                }
            });

            soxProcess.on('error', () => {
                fallbackToFFmpeg('Sox not found');
            });
        });
    }
    
    /**
     * Configure a fluent-ffmpeg command for the user's chosen output format.
     * Shared by mastering and plain conversion so every path honors the setting.
     */
    static applyOutputFormat(command, settings = {}) {
        const format = this.resolveOutputFormat(settings);
        const quality = settings?.output?.quality || 'high';

        switch (format) {
            case 'wav':
                return command.format('wav').audioCodec('pcm_s16le');

            case 'flac': {
                const flacCompression = quality === 'maximum' ? 8 : quality === 'high' ? 5 : 2;
                return command
                    .format('flac')
                    .audioCodec('flac')
                    .outputOptions('-compression_level', String(flacCompression));
            }

            case 'mp3':
            default: {
                const mp3Bitrate = settings?.output?.mp3Bitrate || 192;
                return command
                    .format('mp3')
                    .audioCodec('libmp3lame')
                    .audioBitrate(`${mp3Bitrate}k`);
            }
        }
    }

    /**
     * Convert to the configured output format without mastering filters.
     * Used when the mastering stage is disabled but the file still needs to
     * land in the user's chosen format.
     */
    static async convertToFormat(inputPath, outputPath, settings = {}) {
        return new Promise((resolve, reject) => {
            const format = this.resolveOutputFormat(settings);

            let command = ffmpeg(inputPath)
                .audioFrequency(44100)
                .audioChannels(2);
            command = this.applyOutputFormat(command, settings);

            command
                .on('start', (commandLine) => {
                    console.log(`🎵 Converting to ${format.toUpperCase()}: ` + commandLine);
                })
                .on('end', () => {
                    console.log(`✨ ${format.toUpperCase()} conversion complete`);
                    resolve();
                })
                .on('error', (err) => {
                    console.error(`❌ ${format.toUpperCase()} conversion failed:`, err.message);
                    reject(err);
                })
                .save(outputPath);
        });
    }
    
    static async processWithFFmpegOnly(inputPath, outputPath, influences) {
        return new Promise((resolve, reject) => {
            // FFmpeg fallback: tape-saturation emulation via soft clipping + gentle compand
            const ratio = influences.compand?.ratio || 3;
            const attackMs = Math.round((influences.compand?.attack || 0.06) * 1000);
            const soxLikeFilters = [
                'volume=-1.5dB',
                `bass=g=${influences.bass}`,
                `treble=g=${influences.treble}`,
                `aecho=${influences.echo.delay}:${influences.echo.decay}:6:0.03`,
                'acrusher=bits=16:mode=log:aa=1',  // Subtle bit-crush for tape grit
                `acompressor=threshold=-22dB:ratio=${ratio}:attack=${attackMs}:release=120:makeup=2`
            ].join(',');
            
            ffmpeg(inputPath)
                .audioFilters(soxLikeFilters)
                .audioFrequency(44100)
                .audioChannels(2)
                .format('aiff')
                .on('start', (commandLine) => {
                    console.log('🎛️ FFmpeg sox-like processing: ' + commandLine);
                })
                .on('end', () => {
                    console.log('✨ FFmpeg sox-like processing complete');
                    resolve();
                })
                .on('error', (err) => {
                    console.error('❌ FFmpeg sox-like processing failed:', err.message);
                    reject(err);
                })
                .save(outputPath);
        });
    }
    
    static async processFFmpeg(inputPath, outputPath, settings = {}) {
        return new Promise((resolve, reject) => {
            const format = this.resolveOutputFormat(settings);
            const sampleRate = settings?.output?.sampleRate || 44100;

            // TAPE-CASSETTE MASTERING CHAIN:
            // Warm EQ → Gentle glue compression → Loudness → Single safety limiter
            const filterComplex = [
                // 1. TAPE-STYLE EQUALIZATION: Warm low-mids, gentle high rolloff
                'equalizer=f=60:t=q:w=1.2:g=1.0',      // Sub warmth (tape head bump)
                'equalizer=f=200:t=q:w=1:g=0.8',       // Body / thickness
                'equalizer=f=2500:t=q:w=1:g=0.4',      // Presence without harshness
                'equalizer=f=8000:t=q:w=1:g=-0.8',     // Gentle tape rolloff (no brittle highs)

                // 2. GLUE COMPRESSION: Musical, slow, barely-there
                //    Higher threshold (-22dB) = only catches louder passages
                //    Low ratio (2:1) = transparent glue, not squash
                //    Slow attack (15ms) = transients breathe through
                //    Long release (120ms) = natural pump
                //    Low makeup (+2dB) = subtle leveling
                'acompressor=threshold=-22dB:ratio=2:attack=15:release=120:makeup=2',

                // 3. LOUDNESS NORMALIZATION: EBU R128 streaming standard
                //    -16 LUFS integrated, -1.5dB true peak, moderate dynamics
                'loudnorm=I=-16:TP=-1.5:LRA=11',

                // 4. SAFETY LIMITER: Single final ceiling at -0.3dB (97%)
                //    Protects against inter-sample peaks for lossy codecs
                'alimiter=limit=0.97'
            ].join(',');
            
            let command = ffmpeg(inputPath)
                .complexFilter(filterComplex)
                .audioChannels(2);
            
            // Set sample rate if not preserving original
            if (sampleRate > 0) {
                command = command.audioFrequency(sampleRate);
            }

            // Configure output format (shared with convertToFormat)
            command = this.applyOutputFormat(command, settings);

            command
                .on('start', (commandLine) => {
                    console.log(`🎛️ FFmpeg mastering to ${format.toUpperCase()}: ${commandLine}`);
                })
                .on('progress', (progress) => {
                    if (progress.percent) {
                        process.stdout.write(`\r🎵 Processing: ${Math.round(progress.percent)}%`);
                    }
                })
                .on('end', () => {
                    console.log(`\n✅ FFmpeg mastering complete (${format.toUpperCase()})`);
                    resolve();
                })
                .on('error', (err) => {
                    console.error('\n❌ FFmpeg mastering failed:', err.message);
                    reject(err);
                })
                .save(outputPath);
        });
    }

    static clampNumber(value, min, max, fallback) {
        const numericValue = Number(value);
        if (!Number.isFinite(numericValue)) {
            return fallback;
        }

        return Math.min(max, Math.max(min, numericValue));
    }

    static interpolate(baseValue, targetValue, influence) {
        return baseValue + ((targetValue - baseValue) * influence);
    }
    
}

module.exports = AudioProcessor;
